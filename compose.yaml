services:
  box:
    build:
      context: .
      dockerfile: Dockerfile
    image: box
    working_dir: /workspace
    volumes:
      - .:/workspace
      - box-home:/home/node
    stdin_open: true
    tty: true

volumes:
  box-home:
